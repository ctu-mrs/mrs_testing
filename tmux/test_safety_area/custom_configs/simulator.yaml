simulation_rate: 250.0 # Hz
clock_rate: 250.0 # Hz
realtime_factor: 1.0

iterate_without_input: true

takeoff_patch:
  enabled: false

uav_names: [
  "uav1",
]

uav1:
  type: "t650"
  spawn:
    x: 0.0
    y: 10.0
    z: 1.0
    heading: 0.0
